export const metadata = {
  title: 'WAYummy - Validación de números',
  description: '',
};

export default function ValidateLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
