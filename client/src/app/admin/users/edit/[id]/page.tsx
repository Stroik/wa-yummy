import { PageHeader } from '@/components/PageHeader';

export default function UserEdit() {
  return (
    <section id="user-edit">
      <PageHeader title="Editar usuario" subtitle="Modifique los datos del usuario" />
    </section>
  );
}
